/* eslint-disable react/self-closing-comp */
import React from 'react';
import {View} from 'react-native';

const Space = ({mv, mh}) => {
  return <View style={{marginVertical: mv, marginHorizontal: mh}}></View>;
};

export default Space;
